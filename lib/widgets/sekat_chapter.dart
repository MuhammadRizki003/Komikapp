import 'package:flutter/material.dart';

class SekatChapter extends StatelessWidget {
  const SekatChapter({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.only(bottom: 2),
      child: Container(
        color: Colors.white54,
        height: 2,
        width: double.maxFinite,
      ),
    );
  }
}
